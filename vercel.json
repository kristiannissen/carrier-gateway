{
  "version": 2,
  "builds": [
    {
      "src": "vercel/handler.go",
      "use": "@vercel/go"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "vercel/handler.go" },
    { "src": "/(.*)", "dest": "public/$1" }
  ]
}
