{
  "version": 2,
  "builds": [
    {
      "src": "cmd/vercel/vercel_handler.go",
      "use": "@vercel/go"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "cmd/vercel/vercel_handler.go" },
    { "src": "/(.*)", "dest": "public/$1" }
  ]
}
