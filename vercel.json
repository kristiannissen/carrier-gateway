{
  "version": 2,
  "builds": [
    {
      "src": "cmd/api/main.go",
      "use": "@vercel/go",
      "config": { "functionName": "VercelHandler" }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "cmd/api/main.go" },
    { "src": "/(.*)", "dest": "cmd/api/main.go" }
  ]
}
